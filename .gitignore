# Build artefacts
build/
dist/
*.egg-info/

# Environments and caches
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Scratch folder: local settings, sample payloads, throwaway downloads
.tmp/

# Instrument data and vendor service descriptions - never in the repository
*.tif
*.tiff
*.wsdl

# generated from the code by graphify, and stale the moment anything changes.
# Rebuild it rather than reading a committed copy - the command is in AGENTS.md
graphify-out/

# local agent config, per push-guard - it is this machine, not the project
.claude/
.codex/
